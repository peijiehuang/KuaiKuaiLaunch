<ui:FluentWindow x:Class="KuaiKuaiLaunch.Views.Dialogs.ItemEditDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 mc:Ignorable="d"
                 Title="{Binding Title}"
                 Width="520" 
                 SizeToContent="Height"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 ShowInTaskbar="False"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True">

    <Grid Margin="24,12,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- TitleBar area -->
        <ui:TitleBar Grid.Row="0" Title="{Binding Title}" ShowMaximize="False" ShowMinimize="False" Margin="-24,-12,-24,12" />

        <!-- Form Fields -->
        <StackPanel Grid.Row="1">
            <!-- Name -->
            <TextBlock Text="快捷方式名称 *" FontWeight="SemiBold" Margin="0,0,0,6" />
            <ui:TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="例如：Visual Studio Code" Margin="0,0,0,16" />

            <!-- Target Path -->
            <TextBlock Text="目标路径 (程序/文件/网址) *" FontWeight="SemiBold" Margin="0,0,0,6" />
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:TextBox Grid.Column="0" Text="{Binding TargetPath, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="C:\... 或 https://..." Margin="0,0,8,0" />
                <ui:Button Grid.Column="1" Content="选文件" Command="{Binding BrowseFileCommand}" Margin="0,0,4,0" />
                <ui:Button Grid.Column="2" Content="选目录" Command="{Binding BrowseFolderCommand}" />
            </Grid>

            <!-- Arguments & Working Directory -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="启动参数 (可选)" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <ui:TextBox Text="{Binding Arguments, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="例如：--incognito" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                    <TextBlock Text="起始位置 / 工作目录" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:TextBox Grid.Column="0" Text="{Binding WorkingDirectory, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,4,0" />
                        <ui:Button Grid.Column="1" Content="..." Command="{Binding BrowseWorkDirCommand}" />
                    </Grid>
                </StackPanel>
            </Grid>

            <!-- Custom Icon & Preview -->
            <TextBlock Text="自定义图标 (可选)" FontWeight="SemiBold" Margin="0,0,0,6" />
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:TextBox Grid.Column="0" Text="{Binding CustomIconPath, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="支持 .ico, .png, .exe, .dll" Margin="0,0,8,0" />
                <ui:Button Grid.Column="1" Content="浏览..." Command="{Binding BrowseIconCommand}" Margin="0,0,8,0" />

                <!-- Icon Preview -->
                <Border Grid.Column="2" Width="36" Height="36" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="4" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
                    <Image Source="{Binding IconPreview}" Stretch="Uniform" Margin="4" RenderOptions.BitmapScalingMode="HighQuality" />
                </Border>
            </Grid>

            <!-- Run as Admin & Description -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ui:ToggleSwitch Grid.Column="0" IsChecked="{Binding RunAsAdmin}" Content="以管理员身份运行" VerticalAlignment="Center" Margin="0,0,16,0" />

                <ui:TextBox Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="鼠标悬浮备注说明 (可选)" />
            </Grid>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <ui:Button Content="保存" Appearance="Primary" Command="{Binding SaveCommand}" MinWidth="90" Margin="0,0,8,0" />
            <ui:Button Content="取消" Command="{Binding CancelCommand}" MinWidth="80" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
