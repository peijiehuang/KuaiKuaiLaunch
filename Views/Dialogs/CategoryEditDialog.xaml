<ui:FluentWindow x:Class="KuaiKuaiLaunch.Views.Dialogs.CategoryEditDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:conv="clr-namespace:KuaiKuaiLaunch.Converters"
                 xmlns:vm="clr-namespace:KuaiKuaiLaunch.ViewModels"
                 Title="{Binding Title}"
                 Width="400" 
                 SizeToContent="Height"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 ShowInTaskbar="False"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True">

    <Grid Margin="24,12,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- TitleBar -->
        <ui:TitleBar Grid.Row="0" Title="{Binding Title}" ShowMaximize="False" ShowMinimize="False" Margin="-24,-12,-24,12" />

        <StackPanel Grid.Row="1">
            <TextBlock Text="分类名称 *" FontWeight="SemiBold" Margin="0,0,0,6" />
            <ui:TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="例如：办公工具" Margin="0,0,0,16" />

            <TextBlock Text="分类图标 *" FontWeight="SemiBold" Margin="0,0,0,6" />
            <ComboBox ItemsSource="{Binding AvailableSymbols}" 
                      SelectedItem="{Binding SelectedSymbol}" 
                      Margin="0,0,0,16">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="{Binding Symbol, Converter={x:Static conv:SymbolNameToSymbolRegularConverter.Instance}}" 
                                           FontSize="18" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <ui:Button Content="确定" Appearance="Primary" Command="{Binding SaveCommand}" MinWidth="80" Margin="0,0,8,0" />
            <ui:Button Content="取消" Command="{Binding CancelCommand}" MinWidth="80" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
